<?xml version="1.0" encoding="UTF-8"?>
<!-- TODO ADD dependence
 ref: http://www.avajava.com/tutorials/lessons/how-do-i-deploy-to-tomcat-using-ant.html -->
<project name="deploy_tomcat" default="war" basedir=".">
	<property name="project-name" value="data" />
	<property name="builder" value="xmj" />
	<property name="war-file-name" value="${project-name}.war.zip" />
	<property name="source-directory" value="src" />
	<property name="classes-directory" value="build/classes" />
	<property name="web-directory" value="WebContent" />
	<property name="tomcat-home" value="/Applications/apache-tomcat-7.0.37" />

	<tstamp prefix="build-info">
		<format property="current-date" pattern="d-MMMM-yyyy" locale="en" />
		<format property="current-time" pattern="hh:mm:ss a z" locale="en" />
		<format property="year-month-day" pattern="yyyy-MM-dd" locale="en" />
	</tstamp>
	<property name="build-directory" value="build" />
	<target name="buildPath" depends="">
		<mkdir dir="${build-directory}" />
	</target>

	<path id="compile.classpath">
		<fileset dir="${web-directory}/WEB-INF/lib">
			<include name="*.jar"/>
		</fileset>
		<fileset dir="${tomcat-home}/lib/">
			<include name="*.jar"/>
		</fileset>
	</path>
	<target name="compile" depends="buildPath">
		<javac srcdir="${source-directory}" destdir="${classes-directory}" includeantruntime="true">
			<classpath refid="compile.classpath"/>
		</javac>
	</target>
	<target name="war" depends="compile">

		<delete file="${build-directory}/${war-file-name}" />
		<war warfile="${build-directory}/${war-file-name}" needxmlfile='false'>
			<classes dir="${classes-directory}" />
			<fileset dir="${web-directory}">
			</fileset>
			<manifest>
				<attribute name="Built-By" value="${builder}" />
				<attribute name="Built-On" value="${build-info.current-date}" />
				<attribute name="Built-At" value="${build-info.current-time}" />
			</manifest>
		</war>
	</target>
</project>